/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const VOTE_RECORD_DISCRIMINATOR = new Uint8Array([
  112, 9, 123, 165, 234, 9, 157, 167,
]);

export function getVoteRecordDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VOTE_RECORD_DISCRIMINATOR);
}

export type VoteRecord = {
  discriminator: ReadonlyUint8Array;
  /** The proposal this vote is for */
  proposal: Address;
  /** Nullifier hash - unique per voter per proposal */
  nullifier: ReadonlyUint8Array;
  /** Commitment to the vote choice */
  voteCommitment: ReadonlyUint8Array;
  /** When the vote was cast */
  timestamp: bigint;
  /** Whether the vote has been revealed */
  isRevealed: boolean;
  /** The revealed vote choice (only set after reveal) */
  revealedChoice: Option<number>;
  /** PDA bump seed */
  bump: number;
};

export type VoteRecordArgs = {
  /** The proposal this vote is for */
  proposal: Address;
  /** Nullifier hash - unique per voter per proposal */
  nullifier: ReadonlyUint8Array;
  /** Commitment to the vote choice */
  voteCommitment: ReadonlyUint8Array;
  /** When the vote was cast */
  timestamp: number | bigint;
  /** Whether the vote has been revealed */
  isRevealed: boolean;
  /** The revealed vote choice (only set after reveal) */
  revealedChoice: OptionOrNullable<number>;
  /** PDA bump seed */
  bump: number;
};

/** Gets the encoder for {@link VoteRecordArgs} account data. */
export function getVoteRecordEncoder(): Encoder<VoteRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["proposal", getAddressEncoder()],
      ["nullifier", fixEncoderSize(getBytesEncoder(), 32)],
      ["voteCommitment", fixEncoderSize(getBytesEncoder(), 32)],
      ["timestamp", getI64Encoder()],
      ["isRevealed", getBooleanEncoder()],
      ["revealedChoice", getOptionEncoder(getU8Encoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VOTE_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VoteRecord} account data. */
export function getVoteRecordDecoder(): Decoder<VoteRecord> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["proposal", getAddressDecoder()],
    ["nullifier", fixDecoderSize(getBytesDecoder(), 32)],
    ["voteCommitment", fixDecoderSize(getBytesDecoder(), 32)],
    ["timestamp", getI64Decoder()],
    ["isRevealed", getBooleanDecoder()],
    ["revealedChoice", getOptionDecoder(getU8Decoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link VoteRecord} account data. */
export function getVoteRecordCodec(): Codec<VoteRecordArgs, VoteRecord> {
  return combineCodec(getVoteRecordEncoder(), getVoteRecordDecoder());
}

export function decodeVoteRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VoteRecord, TAddress>;
export function decodeVoteRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VoteRecord, TAddress>;
export function decodeVoteRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<VoteRecord, TAddress> | MaybeAccount<VoteRecord, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVoteRecordDecoder(),
  );
}

export async function fetchVoteRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VoteRecord, TAddress>> {
  const maybeAccount = await fetchMaybeVoteRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVoteRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VoteRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVoteRecord(maybeAccount);
}

export async function fetchAllVoteRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VoteRecord>[]> {
  const maybeAccounts = await fetchAllMaybeVoteRecord(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVoteRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VoteRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVoteRecord(maybeAccount));
}
