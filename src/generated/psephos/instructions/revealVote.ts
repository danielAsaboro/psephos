/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { PSEPHOS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REVEAL_VOTE_DISCRIMINATOR = new Uint8Array([
  100, 157, 139, 17, 186, 75, 185, 149,
]);

export function getRevealVoteDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REVEAL_VOTE_DISCRIMINATOR);
}

export type RevealVoteInstruction<
  TProgram extends string = typeof PSEPHOS_PROGRAM_ADDRESS,
  TAccountRevealer extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountVoteRecord extends string | AccountMeta<string> = string,
  TAccountResults extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRevealer extends string
        ? WritableSignerAccount<TAccountRevealer> &
            AccountSignerMeta<TAccountRevealer>
        : TAccountRevealer,
      TAccountProposal extends string
        ? ReadonlyAccount<TAccountProposal>
        : TAccountProposal,
      TAccountVoteRecord extends string
        ? WritableAccount<TAccountVoteRecord>
        : TAccountVoteRecord,
      TAccountResults extends string
        ? WritableAccount<TAccountResults>
        : TAccountResults,
      ...TRemainingAccounts,
    ]
  >;

export type RevealVoteInstructionData = {
  discriminator: ReadonlyUint8Array;
  voteChoice: number;
  voterSecret: ReadonlyUint8Array;
};

export type RevealVoteInstructionDataArgs = {
  voteChoice: number;
  voterSecret: ReadonlyUint8Array;
};

export function getRevealVoteInstructionDataEncoder(): FixedSizeEncoder<RevealVoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["voteChoice", getU8Encoder()],
      ["voterSecret", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: REVEAL_VOTE_DISCRIMINATOR }),
  );
}

export function getRevealVoteInstructionDataDecoder(): FixedSizeDecoder<RevealVoteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["voteChoice", getU8Decoder()],
    ["voterSecret", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getRevealVoteInstructionDataCodec(): FixedSizeCodec<
  RevealVoteInstructionDataArgs,
  RevealVoteInstructionData
> {
  return combineCodec(
    getRevealVoteInstructionDataEncoder(),
    getRevealVoteInstructionDataDecoder(),
  );
}

export type RevealVoteInput<
  TAccountRevealer extends string = string,
  TAccountProposal extends string = string,
  TAccountVoteRecord extends string = string,
  TAccountResults extends string = string,
> = {
  revealer: TransactionSigner<TAccountRevealer>;
  proposal: Address<TAccountProposal>;
  voteRecord: Address<TAccountVoteRecord>;
  results: Address<TAccountResults>;
  voteChoice: RevealVoteInstructionDataArgs["voteChoice"];
  voterSecret: RevealVoteInstructionDataArgs["voterSecret"];
};

export function getRevealVoteInstruction<
  TAccountRevealer extends string,
  TAccountProposal extends string,
  TAccountVoteRecord extends string,
  TAccountResults extends string,
  TProgramAddress extends Address = typeof PSEPHOS_PROGRAM_ADDRESS,
>(
  input: RevealVoteInput<
    TAccountRevealer,
    TAccountProposal,
    TAccountVoteRecord,
    TAccountResults
  >,
  config?: { programAddress?: TProgramAddress },
): RevealVoteInstruction<
  TProgramAddress,
  TAccountRevealer,
  TAccountProposal,
  TAccountVoteRecord,
  TAccountResults
> {
  // Program address.
  const programAddress = config?.programAddress ?? PSEPHOS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    revealer: { value: input.revealer ?? null, isWritable: true },
    proposal: { value: input.proposal ?? null, isWritable: false },
    voteRecord: { value: input.voteRecord ?? null, isWritable: true },
    results: { value: input.results ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.revealer),
      getAccountMeta(accounts.proposal),
      getAccountMeta(accounts.voteRecord),
      getAccountMeta(accounts.results),
    ],
    data: getRevealVoteInstructionDataEncoder().encode(
      args as RevealVoteInstructionDataArgs,
    ),
    programAddress,
  } as RevealVoteInstruction<
    TProgramAddress,
    TAccountRevealer,
    TAccountProposal,
    TAccountVoteRecord,
    TAccountResults
  >);
}

export type ParsedRevealVoteInstruction<
  TProgram extends string = typeof PSEPHOS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    revealer: TAccountMetas[0];
    proposal: TAccountMetas[1];
    voteRecord: TAccountMetas[2];
    results: TAccountMetas[3];
  };
  data: RevealVoteInstructionData;
};

export function parseRevealVoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevealVoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      revealer: getNextAccount(),
      proposal: getNextAccount(),
      voteRecord: getNextAccount(),
      results: getNextAccount(),
    },
    data: getRevealVoteInstructionDataDecoder().decode(instruction.data),
  };
}
